// O volume do cilindro é igual ao produto do π (PI) pelo quadrado do raio a multiplicar pela altura do cilindro >  é π r² h

function volumeDoCilindro(raio, altura) {
  return Math.PI * raio ** 2 * altura;
}
