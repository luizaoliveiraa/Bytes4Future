// Cria uma função que recebe um array como argumento e retorna um array com os elementos que são inteiros.
// Utiliza o que aprendeste sobre o filter para resolver este exercício.

function filtraInteiros(array) {
  return array.filter((elemento) => Number.isInteger(elemento));
}

// Ouu

array.filter((elemento) => e.isInteger);
