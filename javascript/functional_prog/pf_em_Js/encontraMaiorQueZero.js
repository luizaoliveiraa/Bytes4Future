// 13. Cria uma função que recebe um array como argumento e retorna o primeiro elemento maior que 0.
// Utiliza o que aprendeste sobre o find para resolver este exercício.

function encontraMaiorQueZero(array) {
  // escreve aqui a função
  return array.find((element) => element > 0);
}
