// Cria uma função que recebe um array como argumento e retorna um array com os elementos cujo o módulo da divisão por 2 é diferente de zero.
// Utiliza o que aprendeste sobre o filter para resolver este exercício.

function filtraArray(array) {
  return array.filter((elemento) => elemento % 2 !== 0);
}
