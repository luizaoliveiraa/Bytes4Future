// const exe1 = () => {}

// let resultado = 0

// function soma(a, b) {
    
//     // resultado = a + b //Efeito, alterou variavel
//     return a + b //Resultado
// }

// resultado = soma(3, 2)

function soma(a, b) {
    return a + b
}
function multiplica(a, b) {
    return a * b
}
function divide(a, b) {
    return a / b
}
function subtrai(a, b) {
    return a - b
}

// let lRetangulo = 3
// let cRetangulo = 5

let rectangle = {
    length: 5,
    width: 3
}

let rectArea = multiplica(rectangle.length, rectangle.width)
let triArea = divide(rectArea, 2)

let tri2Area = divide(multiplica(rectangle.length, rectangle.width), 2)



console.log(a)

const a = 3


