export function findUserByEmail(email) {
    //Ligar a base de dados
    //basededados.find(user => user.email === email)
}

export function insertUser(user) {
    //Ligar a base de dados
    //basededados.push(user)
}